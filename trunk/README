# README

This package implements an Erlang linked in device driver and API for
the Netaccess(TM) WAN Cards from Brooktrout Technology.  

To build:

	./configure
	make
	make install

Options:

	--enable-threads          Build with thread support.  This is 
                             needed for full functionality.

	--enable-debug            For development and testing.


